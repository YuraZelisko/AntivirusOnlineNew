package com.antivirus.validator.RegionValidator;

public interface RegionValidationMessages {
    String EMPTY_REGIONNAME_FIELD = "EMPTY REGION NAME";
    String REGIONNAME_ALREADY_EXISTS = "REGION NAME ALREADY EXISTS";
}
